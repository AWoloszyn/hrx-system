################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/binding/c/example/cxx/BUILD.bazel                                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
iree_c_embed_data(
  NAME
    kernels
  SRCS
    "kernels.cpp"
  C_FILE_OUTPUT
    "kernels.c"
  H_FILE_OUTPUT
    "kernels.h"
  IDENTIFIER
    "loomc_cxx_example_kernels"
  FLATTEN
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU)
loom_cc_binary(
  NAME
    jit_amdgpu
  SRCS
    "jit_amdgpu.c"
  DEPS
    ::kernels
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers
    loom::binding::c::import::cxx
    loom::binding::c::loomc
    loom::binding::c::target::amdgpu
    loom::binding::c::target::amdgpu::iree_hal
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_AMDGPU AND LOOM_EMIT_AMDGPU AND LOOM_EXECUTE_IREE_HAL AND IREE_HAL_DRIVER_AMDGPU AND LOOM_IMPORT_CXX_EMBED_INCLUDES)
loom_cc_test(
  NAME
    jit_amdgpu_test
  SRCS
    "jit_amdgpu.c"
  DEPS
    ::kernels
    iree::async
    iree::async::util::proactor_pool
    iree::base
    iree::base::threading
    iree::hal
    iree::hal::drivers
    loom::binding::c::import::cxx
    loom::binding::c::loomc
    loom::binding::c::target::amdgpu
    loom::binding::c::target::amdgpu::iree_hal
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  SANITIZER_SUPPRESSIONS
    lsan
    rocm
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
