################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/import/cxx/BUILD.bazel                                         #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_IMPORT_CXX)
loom_generated_cc_library(
  NAME
    scalar_bindings
  GENERATOR
    loom::py::loom::gen::cxx::intrinsics_generator
  HDRS
    "scalar_bindings.h"
  DEPS
    loom::ops::op_defs
    loom::ops::scalar
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_IMPORT_CXX_EMBED_INCLUDES)
iree_c_embed_data(
  NAME
    embedded_loomcxx_headers
  SRCS
    "include/loomcxx/kernel.h"
    "include/loomcxx/math.h"
    "include/loomcxx/scalar.h"
  C_FILE_OUTPUT
    "embedded_loomcxx_headers.c"
  H_FILE_OUTPUT
    "embedded_loomcxx_headers.h"
  IDENTIFIER
    "loom_cxx_embedded_loomcxx_headers"
  FLATTEN
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_IMPORT_CXX_EMBED_INCLUDES)
iree_c_embed_data(
  NAME
    embedded_hip_headers
  SRCS
    "include/hip/hip_fp16.h"
    "include/hip/hip_runtime.h"
  C_FILE_OUTPUT
    "embedded_hip_headers.c"
  H_FILE_OUTPUT
    "embedded_hip_headers.h"
  IDENTIFIER
    "loom_cxx_embedded_hip_headers"
  FLATTEN
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX)
set(_include_catalog_platform_defines "")
if(LOOM_IMPORT_CXX_EMBED_INCLUDES)
  list(APPEND _include_catalog_platform_defines "LOOM_CXX_EMBED_INCLUDES=1")
else()
  list(APPEND _include_catalog_platform_defines "LOOM_CXX_EMBED_INCLUDES=0")
endif()
set(_include_catalog_platform_deps "")
if(LOOM_IMPORT_CXX_EMBED_INCLUDES)
  list(APPEND _include_catalog_platform_deps ::embedded_hip_headers)
  list(APPEND _include_catalog_platform_deps ::embedded_loomcxx_headers)
endif()
loom_cc_library(
  NAME
    include_catalog
  HDRS
    "include_catalog.h"
  SRCS
    "include_catalog.cc"
  DEPS
    ${_include_catalog_platform_deps}
  DEFINES
    ${_include_catalog_platform_defines}
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX)
loom_cc_test(
  NAME
    import_test
  SRCS
    "import_test.cc"
  DEPS
    ::cxx
    ::include_catalog
    iree::testing::gtest
    iree::testing::gtest_main
    loom::format::text::printer
    loom::ops::op_registry
  LABELS
    "iree-build-requirement=loom.import.cxx"
)
endif()

if(LOOM_IMPORT_CXX)
loom_cc_library(
  NAME
    cxx
  HDRS
    "import.h"
  SRCS
    "failure.h"
    "import.cc"
    "intrinsics.cc"
    "intrinsics.h"
    "mutations.h"
    "source.cc"
    "source.h"
    "translation.cc"
    "translation.h"
  DEPS
    ::include_catalog
    ::scalar_bindings
    iree::base
    iree::base::internal::arena
    iree::third_party::cxx_parser
    loom::error::diagnostic
    loom::error::error_defs
    loom::ir
    loom::ops::buffer
    loom::ops::config
    loom::ops::func
    loom::ops::index
    loom::ops::kernel
    loom::ops::scalar
    loom::ops::scf
    loom::ops::view
    loom::verify
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# The parser uses C++23 and C++ exceptions internally. These requirements do not
# escape the C import API or affect other Loom targets.
if(TARGET loom_import_cxx_cxx.objects)
  target_compile_features(loom_import_cxx_cxx.objects PRIVATE cxx_std_23)
  if(MSVC)
    target_compile_options(loom_import_cxx_cxx.objects PRIVATE /EHsc /GR)
  else()
    target_compile_options(loom_import_cxx_cxx.objects PRIVATE -fexceptions -frtti)
  endif()
endif()
