################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/import/cxx/tooling/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_IMPORT_CXX)
loom_cc_test(
  NAME
    input_test
  SRCS
    "input_test.cc"
  DEPS
    ::input
    ::source_capture_test_data
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    iree::testing::gtest
    iree::testing::gtest_main
    iree::testing::temp_file
    loom::error::source
    loom::format::location
    loom::import::cxx
    loom::import::cxx::source::catalog
    loom::import::cxx::source::options
    loom::ir
    loom::ops::func
    loom::ops::func::location_capture
    loom::ops::op_defs
    loom::ops::op_registry
    loom::testing::test_file
    loom::tooling::input
    loom::tools::loom-check::execute
    loom::tools::loom-check::file
    loom::tools::loom-check::test_util
    loom::util::json
    loom::util::stream
  LABELS
    "iree-build-requirement=loom.import.cxx"
)
endif()

iree_c_embed_data(
  NAME
    source_capture_test_data
  SRCS
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/test/source_capture.cxx-test"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/test/source_capture.h"
  C_FILE_OUTPUT
    "source_capture_test_data.c"
  H_FILE_OUTPUT
    "source_capture_test_data.h"
  IDENTIFIER
    "loom_cxx_source_capture_test_data"
  TESTONLY
  PUBLIC
)

if(LOOM_IMPORT_CXX)
loom_cc_library(
  NAME
    input
  HDRS
    "input.h"
  SRCS
    "input.c"
  DEPS
    iree::base
    iree::base::internal::arena
    iree::base::internal::path
    loom::import::cxx
    loom::import::cxx::source::options
    loom::tooling::input
    loom::tooling::io::file
  PUBLIC
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_VM)
iree_execution_test_suite(
  NAME
    execution_test
  MANIFESTS
    "execution.test.json"
  TOOLS
    "iree-benchmark-loom=loom::tools::iree-benchmark-loom"
    "iree-test-loom=loom::tools::iree-test-loom"
    "loom-link=loom::tools::loom-link"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/check_cases.cc"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/check_cases.h"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/check_library.cc"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/source_input.cc"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/source_input.h"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/source_input.loom"
)
endif()

if(LOOM_IMPORT_CXX AND LOOM_TARGET_ARCH_VM)
loom_test(
  NAME
    source_test
  SRCS
    "check_cases.cc"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/import/cxx/tooling/check_cases.h"
  INPUTOPTS
    "cxx:D=CHECK_EXPECTED=0u"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
