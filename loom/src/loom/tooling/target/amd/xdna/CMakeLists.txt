################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tooling/target/amd/xdna/BUILD.bazel                            #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
loom_cc_library(
  NAME
    artifact_provider
  HDRS
    "artifact_provider.h"
  SRCS
    "artifact_provider.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::target::arch::amd::xdna::aie2p::artifact
    loom::target::arch::amd::xdna::aie2p::low_registry
    loom::target::arch::amd::xdna::aie2p::target_profile
    loom::target::arch::amd::xdna::aie2p::target_records
    loom::target::entry_selection
    loom::tooling::compile::artifact
  PUBLIC
)
endif()

if(LOOM_BUILD AND LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
iree_execution_test_suite(
  NAME
    pipeline_record_shapes_test
  MANIFESTS
    "pipeline-record-shapes.test.json"
  TOOLS
    "loom-compile=loom::tools::loom-compile"
  DATA
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amd/xdna/test/equal_count_different_shape.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amd/xdna/test/extra_temporal_axis.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amd/xdna/test/missing_lane_axis.loom"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amd/xdna/test/missing_temporal_axis.loom"
  LABELS
    "hostonly"
)
endif()

if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
loom_check_test_suite(
  NAME
    pipeline_diagnostics_test
  SRCS
    "test/unit_lane_extent.loom-test"
  RUNNER
    loom::target::arch::amd::xdna::aie2p::loom-check
)
endif()

if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
loom_check_compile_tests(
  NAME
    "test_unit_lane_extent"
  SRC
    "test/unit_lane_extent.loom-test"
  REGISTERED_SRCS
    "test/unit_lane_extent.loom-test"
  TARGETS
    loom::target::xdna::amd.xdna.strix_halo.17f0_11
)
endif()

if(LOOM_TARGET_ARCH_XDNA AND LOOM_EMIT_XDNA)
iree_py_test(
  NAME
    publication_test
  MAIN
    "test/publication_test.py"
  SRCS
    "test/publication_test.py"
  ARGS
    "$<TARGET_FILE:loom::tools::loom-compile::loom-compile>"
    "${PROJECT_SOURCE_DIR}/loom/src/loom/tooling/target/amd/xdna/test/unit_lane_extent.loom-test"
  DEPS
    loom::tools::loom-compile::loom-compile
  LABELS
    "hostonly"
  PACKAGE_DIRS
    "${PROJECT_SOURCE_DIR}/loom/py"
    "${PROJECT_BINARY_DIR}/loom/py"
    "${PROJECT_SOURCE_DIR}"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
