// RUN: emit source-low output=none

amdgpu.target<gfx11-generic> @target

func.def target(@target) @zero_divisor(%value: i32) -> (i32) {
  %c0 = scalar.constant 0 : i32
  // ERROR@+1: TARGET/003 {op_name="scalar.remui"}
  %result = scalar.remui %value, %c0 : i32
  func.return %result : i32
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @dynamic_divisor(%value: i32, %divisor: i32) -> (i32) {
  // ERROR@+1: TARGET/003 {op_name="scalar.remui"}
  %result = scalar.remui %value, %divisor : i32
  func.return %result : i32
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @wide_divisor(%value: i64) -> (i64) {
  %c3 = scalar.constant 3 : i64
  // ERROR@+1: TARGET/002 {op_name="scalar.remui"}
  %result = scalar.remui %value, %c3 : i64
  func.return %result : i64
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @wide_dynamic_divisor(%value: i64, %divisor: i64) -> (i64) {
  // ERROR@+1: TARGET/002 {op_name="scalar.remui"}
  %result = scalar.remui %value, %divisor : i64
  func.return %result : i64
}

// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @signed_remainder(%value: i32) -> (i32) {
  %c3 = scalar.constant 3 : i32
  // ERROR@+1: TARGET/001 {op_name="scalar.remsi"}
  %result = scalar.remsi %value, %c3 : i32
  func.return %result : i32
}
// ====

amdgpu.target<gfx11-generic> @target

func.def target(@target) @unsigned_quotient(%value: i32) -> (i32) {
  %c3 = scalar.constant 3 : i32
  // ERROR@+1: TARGET/001 {op_name="scalar.divui"}
  %result = scalar.divui %value, %c3 : i32
  func.return %result : i32
}
