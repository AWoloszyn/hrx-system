// RUN: emit source-low output=low

aie2p.target<core> @target

func.def target(@target) @store_immediate(%value: vector<64xi32>, %output: buffer) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.constant 256 : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xi32>
  %extent = index.constant 8 : index
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32>
  func.return
}

func.def target(@target) @store_materialized(%value: vector<64xi32>, %output: buffer) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.constant 512 : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xi32>
  %extent = index.constant 8 : index
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32>
  func.return
}

func.def target(@target) @store_dynamic(%value: vector<64xi32>, %output: buffer, %base_argument: offset) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.assume %base_argument [range(%base_argument, 0, 768), mul(%base_argument, 64)] : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xi32>
  %extent = index.constant 8 : index
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32>
  func.return
}

func.def target(@target) @store_nested(%value: vector<64xi32>, %output: buffer, %base_argument: offset) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.assume %base_argument [range(%base_argument, 0, 768), mul(%base_argument, 64)] : offset
  %parent = buffer.view %aligned[%base] : buffer -> view<16x8xi32>
  %view = view.subview %parent[8, 0] : view<16x8xi32> -> view<8x8xi32>
  %extent = index.constant 8 : index
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32>
  func.return
}

func.def target(@target) @store_private_f32(%value: vector<64xf32>) {
  %bytes = index.constant 512 : offset
  %output = buffer.alloca<private> align(64) %bytes : buffer
  %base = index.constant 256 : offset
  %view = buffer.view %output[%base] : buffer -> view<8x8xf32>
  %extent = index.constant 8 : index
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xf32>, view<8x8xf32>
  func.return
}

// ----
low.func.def target<amd.xdna.aie2p.core>(@target) abi(object_function) @store_immediate(%value: reg<aie2p.mbms x4>) asm {
  %view = resource<native_pointer> {index = 1, source_type = buffer} : reg<aie2p.ep>
  %35 = slice %value[0] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %35, %view, 256
  %36 = slice %value[1] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %36, %view, 320
  %37 = slice %value[2] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %37, %view, 384
  %38 = slice %value[3] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %38, %view, 448
  return
}

low.func.def target<amd.xdna.aie2p.core>(@target) abi(object_function) @store_materialized(%value: reg<aie2p.mbms x4>) asm {
  %view = resource<native_pointer> {index = 1, source_type = buffer} : reg<aie2p.ep>
  %41 = slice %value[0] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %42 = mov.static-byte-offset 512
  %43 = mov.address-index %42
  vst.acc.index %41, %view, %43
  %44 = slice %value[1] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %45 = mov.static-byte-offset 576
  %46 = mov.address-index %45
  vst.acc.index %44, %view, %46
  %47 = slice %value[2] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %48 = mov.static-byte-offset 640
  %49 = mov.address-index %48
  vst.acc.index %47, %view, %49
  %50 = slice %value[3] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %51 = mov.static-byte-offset 704
  %52 = mov.address-index %51
  vst.acc.index %50, %view, %52
  return
}

low.func.def target<amd.xdna.aie2p.core>(@target) abi(object_function) @store_dynamic(%value: reg<aie2p.mbms x4>, %base: reg<aie2p.er>) asm {
  %view = resource<native_pointer> {index = 1, source_type = buffer} : reg<aie2p.ep>
  %56 = slice %value[0] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %57 = mov.address-index %base
  vst.acc.index %56, %view, %57
  %58 = slice %value[1] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %59 = mov.static-byte-offset 64
  %60 = add.rr %base, %59
  %61 = mov.address-index %60
  vst.acc.index %58, %view, %61
  %62 = slice %value[2] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %63 = mov.static-byte-offset 128
  %64 = add.rr %base, %63
  %65 = mov.address-index %64
  vst.acc.index %62, %view, %65
  %66 = slice %value[3] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %67 = mov.static-byte-offset 192
  %68 = add.rr %base, %67
  %69 = mov.address-index %68
  vst.acc.index %66, %view, %69
  return
}

low.func.def target<amd.xdna.aie2p.core>(@target) abi(object_function) @store_nested(%value: reg<aie2p.mbms x4>, %base: reg<aie2p.er>) asm {
  %view = resource<native_pointer> {index = 1, source_type = buffer} : reg<aie2p.ep>
  %73 = slice %value[0] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %74 = mov.static-byte-offset 256
  %75 = add.rr %base, %74
  %76 = mov.address-index %75
  vst.acc.index %73, %view, %76
  %77 = slice %value[1] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %78 = mov.static-byte-offset 320
  %79 = add.rr %base, %78
  %80 = mov.address-index %79
  vst.acc.index %77, %view, %80
  %81 = slice %value[2] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %82 = mov.static-byte-offset 384
  %83 = add.rr %base, %82
  %84 = mov.address-index %83
  vst.acc.index %81, %view, %84
  %85 = slice %value[3] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  %86 = mov.static-byte-offset 448
  %87 = add.rr %base, %86
  %88 = mov.address-index %87
  vst.acc.index %85, %view, %88
  return
}

low.func.def target<amd.xdna.aie2p.core>(@target) abi(object_function) @store_private_f32(%value: reg<aie2p.mbms x4>) asm {
  %90 = storage {byte_alignment = 64, byte_length = 512} : low.storage<private>
  %view = storage_address %90 : low.storage<private> -> reg<aie2p.ep>
  %92 = slice %value[0] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %92, %view, 256
  %93 = slice %value[1] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %93, %view, 320
  %94 = slice %value[2] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %94, %view, 384
  %95 = slice %value[3] : reg<aie2p.mbms x4> -> reg<aie2p.mbms>
  vst.acc %95, %view, 448
  return
}

// ====
// RUN: emit source-low output=none

aie2p.target<core> @target

func.def target(@target) @unaligned_store(%value: vector<64xi32>, %output: buffer) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.constant 4 : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xi32>
  %extent = index.constant 8 : index
  // ERROR@+1: TARGET/008
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32>
  func.return
}

// ====
// RUN: emit source-low output=none

aie2p.target<core> @target

func.def target(@target) @strided_store(%value: vector<64xi32>, %output: buffer) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.constant 0 : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xi32, #encoding.layout.strided<strides=[16, 1]>>
  %extent = index.constant 8 : index
  // ERROR@+1: TARGET/008
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xi32, #encoding.layout.strided<strides=[16, 1]>>
  func.return
}

// ====
// RUN: emit source-low output=none

aie2p.target<core> @target

func.def target(@target) @requires_numeric_conversion(%value: vector<64xi32>, %output: buffer) {
  %aligned = buffer.assume.alignment %output {minimum_alignment = 64} : buffer
  %base = index.constant 0 : offset
  %view = buffer.view %aligned[%base] : buffer -> view<8x8xf32>
  %extent = index.constant 8 : index
  // ERROR@+1: TARGET/002
  vector.fragment.store<result> %value, %view[0, 0] shape [%extent, %extent] : vector<64xi32>, view<8x8xf32>
  func.return
}
