// Copyright 2026 The IREE Authors
//
// Licensed under the Apache License v2.0 with LLVM Exceptions.
// See https://llvm.org/LICENSE.txt for license information.
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

// A 64-bit aperture read supplies the high address word. The low word is zero;
// a 32-bit move from the same inline source cannot construct a flat LDS pointer.
// RUN: emit amdgpu-native-words @shared_base_gfx942 strategy=source

amdgpu.target<gfx942> @target

low.func.def target<amdgpu.cdna3.core>(@target) @shared_base_gfx942() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBF810000

// ====

// RUN: emit amdgpu-native-words @shared_base_gfx950 strategy=source

amdgpu.target<gfx950> @target

low.func.def target<amdgpu.cdna4.core>(@target) @shared_base_gfx950() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBF810000

// ====

// RUN: emit amdgpu-native-words @shared_base_gfx1100 strategy=source

amdgpu.target<gfx1100> @target

low.func.def target<amdgpu.rdna3.core>(@target) @shared_base_gfx1100() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBFB00000

// ====

// RUN: emit amdgpu-native-words @shared_base_gfx1151 strategy=source

amdgpu.target<gfx1151> @target

low.func.def target<amdgpu.rdna3_5.core>(@target) @shared_base_gfx1151() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBFB00000

// ====

// RUN: emit amdgpu-native-words @shared_base_gfx1201 strategy=source

amdgpu.target<gfx1201> @target

low.func.def target<amdgpu.rdna4.core>(@target) @shared_base_gfx1201() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBFB00000

// ====

// RUN: emit amdgpu-native-words @shared_base_gfx1250 strategy=source

amdgpu.target<gfx1250> @target

low.func.def target<amdgpu.rdna4.gfx125x.core>(@target) @shared_base_gfx1250() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
0xBE8001EB
0xBFB00000

// ====

// RUN: emit amdgpu-assembly @shared_base_assembly strategy=source

amdgpu.target<gfx1151> @target

low.func.def target<amdgpu.rdna3_5.core>(@target) @shared_base_assembly() -> (reg<amdgpu.sgpr>) asm {
  %aperture = s_mov_b64_shared_base
  %upper = slice %aperture[1] : reg<amdgpu.sgpr x2> -> reg<amdgpu.sgpr>
  return %upper
}

// ----
  s_mov_b64 s[0:1], src_shared_base
  s_endpgm
