################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/cts/gpu/BUILD.bazel                                                  #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    gpu_device_fixture
  HDRS
    "gpu_device_fixture.h"
  DEPS
    amdf::headers
    iree::third_party::google_test
    libamdf::cts::util::device_cache
    libamdf::cts::util::provider_headers
  TESTONLY
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    linux_tests
  HDRS
    "target/gfx1151/user_queue_memory_test.h"
  SRCS
    "memory_linux_test.cc"
    "memory_xdna_interop_linux_test.cc"
    "target/gfx1151/user_queue_memory_test.cc"
  DEPS
    ::gpu_device_fixture
    amdf::headers
    iree::third_party::google_test
    libamdf::cts::util::device_cache
    libamdf::cts::util::provider_headers
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Windows" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    windows_tests
  SRCS
    "kernel_queue_test.cc"
    "memory_interop_test.cc"
    "windows/memory_test.cc"
  DEPS
    ::gpu_device_fixture
    amdf::headers
    iree::third_party::google_test
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_gpu_tests_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _gpu_tests_platform_deps ::linux_tests)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _gpu_tests_platform_deps ::windows_tests)
endif()
iree_cc_library(
  NAME
    gpu_tests
  SRCS
    "gpu_extension_test.cc"
    "memory_group_test.cc"
  DEPS
    ::gpu_device_fixture
    amdf::headers
    iree::third_party::google_test
    libamdf::cts::util::device_cache
    libamdf::cts::util::provider_headers
    ${_gpu_tests_platform_deps}
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    gpu_static_bin
  DEPS
    ::gpu_tests
    amdf::headers
    libamdf::cts::util::static_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_static"
  ARGS
    "--amdf_native_lifetime=process"
  SRC
    ::gpu_static_bin
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_static_instance"
  ARGS
    "--amdf_native_lifetime=instance"
  SRC
    ::gpu_static_bin
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    gpu_shared_bin
  DATA
    amdf::amdf
  DEPS
    ::gpu_tests
    amdf::headers
    libamdf::cts::util::shared_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_shared"
  ARGS
    "--amdf_native_lifetime=process"
  SRC
    ::gpu_shared_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_shared_instance"
  ARGS
    "--amdf_native_lifetime=instance"
  SRC
    ::gpu_shared_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    gpu_dynamic_bin
  DATA
    amdf::amdf
  DEPS
    ::gpu_tests
    amdf::headers
    libamdf::cts::util::dynamic_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_dynamic"
  ARGS
    "--amdf_native_lifetime=process"
    "--amdf_library={{$<TARGET_FILE:amdf::amdf>}}"
  SRC
    ::gpu_dynamic_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA) AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "gpu_dynamic_instance"
  ARGS
    "--amdf_native_lifetime=instance"
    "--amdf_library={{$<TARGET_FILE:amdf::amdf>}}"
  SRC
    ::gpu_dynamic_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
    "iree-run-requirement=libamdf.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
