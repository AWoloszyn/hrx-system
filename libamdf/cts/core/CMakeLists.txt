################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/cts/core/BUILD.bazel                                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD)
iree_cc_library(
  NAME
    core_tests
  SRCS
    "allocator_test.cc"
    "discovery_test.cc"
    "memory_test.cc"
    "query_test.cc"
  DEPS
    amdf::headers
    iree::third_party::google_test
    libamdf::cts::util::device_cache
    libamdf::cts::util::provider_headers
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    core_static_bin
  DEPS
    ::core_tests
    amdf::headers
    libamdf::cts::util::static_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_static"
  ARGS
    "--amdf_native_lifetime=process"
  SRC
    ::core_static_bin
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_static_instance"
  ARGS
    "--amdf_native_lifetime=instance"
  SRC
    ::core_static_bin
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    core_shared_bin
  DATA
    amdf::amdf
  DEPS
    ::core_tests
    amdf::headers
    libamdf::cts::util::shared_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_shared"
  ARGS
    "--amdf_native_lifetime=process"
  SRC
    ::core_shared_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_shared_instance"
  ARGS
    "--amdf_native_lifetime=instance"
  SRC
    ::core_shared_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
amdf_cts_binary(
  NAME
    core_dynamic_bin
  DATA
    amdf::amdf
  DEPS
    ::core_tests
    amdf::headers
    libamdf::cts::util::dynamic_provider
    libamdf::cts::util::test_main
  TESTONLY
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_dynamic"
  ARGS
    "--amdf_native_lifetime=process"
    "--amdf_library={{$<TARGET_FILE:amdf::amdf>}}"
  SRC
    ::core_dynamic_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

if(AMDF_BUILD AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
iree_native_test(
  NAME
    "core_dynamic_instance"
  ARGS
    "--amdf_native_lifetime=instance"
    "--amdf_library={{$<TARGET_FILE:amdf::amdf>}}"
  SRC
    ::core_dynamic_bin
  DATA
    "amdf::amdf"
  LABELS
    "iree-build-requirement=libamdf"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
