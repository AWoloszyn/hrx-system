################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/xdna/BUILD.bazel                                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    endpoint_profile
  HDRS
    "endpoint_profile.h"
  SRCS
    "endpoint_profile.c"
  DEPS
    amdf::headers
    libamdf::src::pci
    libamdf::src::xdna::target::npu5::ert_packet
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    device
  HDRS
    "device.h"
    "memory.h"
  SRCS
    "device.c"
    "memory.c"
  DEPS
    ::endpoint_profile
    amdf::headers
    libamdf::src::device
    libamdf::src::endpoint
    libamdf::src::host_mapping
    libamdf::src::memory
    libamdf::src::structure
    libamdf::src::xdna::umd::device
    libamdf::src::xdna::umd::memory
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_library(
  NAME
    extension
  HDRS
    "extension.h"
  SRCS
    "extension.c"
  DEPS
    ::device
    ::endpoint_profile
    amdf::headers
    libamdf::src::endpoint
    libamdf::src::platform
    libamdf::src::structure
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA AND (CMAKE_SYSTEM_NAME STREQUAL "Linux" OR CMAKE_SYSTEM_NAME STREQUAL "Windows"))
set(_xdna_platform_deps "")
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  list(APPEND _xdna_platform_deps libamdf::src::xdna::umd::drm)
elseif(CMAKE_SYSTEM_NAME STREQUAL "Windows")
  list(APPEND _xdna_platform_deps libamdf::src::xdna::umd::mcdm)
endif()
iree_cc_library(
  NAME
    xdna
  DEPS
    ::extension
    amdf::headers
    ${_xdna_platform_deps}
  PUBLIC
)
endif()

if(AMDF_BUILD AND AMDF_FAMILY_XDNA)
iree_cc_test(
  NAME
    endpoint_profile_test
  SRCS
    "endpoint_profile_test.cc"
  DEPS
    ::endpoint_profile
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.xdna"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
