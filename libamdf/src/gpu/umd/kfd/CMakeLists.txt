################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/gpu/umd/kfd/BUILD.bazel                                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    file
  HDRS
    "file.h"
  DEPS
    amdf::headers
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    topology
  HDRS
    "topology.h"
  SRCS
    "topology.c"
  DEPS
    amdf::headers
    libamdf::src::gpu::endpoint_profile
    libamdf::src::platform
    libamdf::src::platform::linux::endpoint
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    reset_monitor
  HDRS
    "reset_monitor.h"
  SRCS
    "reset_monitor.c"
  DEPS
    amdf::headers
    libamdf::src::atomics
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_test(
  NAME
    reset_monitor_test
  SRCS
    "reset_monitor_test.cc"
  DEPS
    ::reset_monitor
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    device_state
  HDRS
    "device.h"
  DEPS
    ::reset_monitor
    ::topology
    amdf::headers
    libamdf::src::gpu::umd::device
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    device
  SRCS
    "device.c"
  DEPS
    ::device_state
    ::file
    ::reset_monitor
    ::topology
    amdf::headers
    libamdf::src::allocator
    libamdf::src::gpu::umd::kfd::target::gfx1151::pm4_queue
    libamdf::src::platform::linux::endpoint
    libamdf::src::platform::linux::file
    libamdf::src::platform::linux::host_cache
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    buffer
  HDRS
    "buffer.h"
  SRCS
    "buffer.c"
  DEPS
    ::device_state
    amdf::headers
    libamdf::src::allocator
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_test(
  NAME
    buffer_test
  SRCS
    "buffer_test.cc"
  LINKOPTS
    "-Wl,--wrap=ioctl"
  DEPS
    ::buffer
    ::device_state
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    libamdf::src::allocator
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    memory
  SRCS
    "memory.c"
  DEPS
    ::buffer
    ::device_state
    ::topology
    amdf::headers
    libamdf::src::allocator
    libamdf::src::gpu::umd::memory
    libamdf::src::platform::linux::dma_buf
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_test(
  NAME
    memory_test
  SRCS
    "memory_test.cc"
  DEPS
    ::device_state
    ::memory
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
    libamdf::src::gpu::umd::memory
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    endpoint_profile
  SRCS
    "endpoint_profile.c"
  DEPS
    ::file
    ::topology
    amdf::headers
    libamdf::src::gpu::endpoint_profile
    libamdf::src::gpu::umd::endpoint_profile
    libamdf::src::platform::linux::file
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    kernel_queue
  SRCS
    "kernel_queue.c"
  DEPS
    amdf::headers
    libamdf::src::gpu::umd::kernel_queue
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    user_queue
  SRCS
    "user_queue.c"
  DEPS
    amdf::headers
    libamdf::src::gpu::umd::user_queue
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    kfd
  DEPS
    ::buffer
    ::device
    ::endpoint_profile
    ::kernel_queue
    ::memory
    ::user_queue
    amdf::headers
  PUBLIC
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
