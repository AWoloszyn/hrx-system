################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libamdf/src/gpu/umd/kfd/target/BUILD.bazel                                   #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    memory
  HDRS
    "memory.h"
  SRCS
    "memory.c"
  DEPS
    amdf::headers
    libamdf::src::gpu::umd::kfd::topology
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_test(
  NAME
    memory_test
  SRCS
    "memory_test.cc"
  DEPS
    ::memory
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    queue_plan
  HDRS
    "queue_plan.h"
  DEPS
    amdf::headers
    libamdf::src::gpu::endpoint_profile
    libamdf::src::gpu::umd::kfd::buffer
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    pm4_queue
  HDRS
    "pm4_queue.h"
  SRCS
    "pm4_queue.c"
  DEPS
    $<BUILD_LOCAL_INTERFACE:iree::third_party::linux_kfd_uapi>
    ::queue_plan
    amdf::headers
    libamdf::src::gpu::umd::kfd::topology
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    sdma_queue
  HDRS
    "sdma_queue.h"
  SRCS
    "sdma_queue.c"
  DEPS
    $<BUILD_LOCAL_INTERFACE:iree::third_party::linux_kfd_uapi>
    ::queue_plan
    amdf::headers
    libamdf::src::gpu::umd::kfd::topology
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_library(
  NAME
    user_queue
  HDRS
    "user_queue.h"
  SRCS
    "user_queue.c"
  DEPS
    ::pm4_queue
    ::queue_plan
    ::sdma_queue
    amdf::headers
    libamdf::src::gpu::umd::kfd::topology
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_ARCH STREQUAL "x86_64" AND AMDF_BUILD AND (AMDF_FAMILY_RDNA OR AMDF_FAMILY_CDNA))
iree_cc_test(
  NAME
    user_queue_test
  SRCS
    "user_queue_test.cc"
  DEPS
    ::user_queue
    amdf::headers
    iree::third_party::google_test
    iree::third_party::google_test_main
  LABELS
    "iree-build-requirement=libamdf"
    "iree-build-requirement=libamdf.gpu"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
