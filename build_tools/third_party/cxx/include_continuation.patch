diff --git a/src/parser/cxx/preprocessor.cc b/src/parser/cxx/preprocessor.cc
--- a/src/parser/cxx/preprocessor.cc
+++ b/src/parser/cxx/preprocessor.cc
@@ -4088,7 +4088,11 @@ void PendingInclude::resolveWith(std::optional<std::string> resolvedFileName,

   auto fileName = resolvedFileName.value();

-  auto resume = [=, this]() -> std::optional<PreprocessingState> {
+  // The continuation outlives this request. Retain the preprocessor and token
+  // location, which belong to the preprocessing invocation, not the request.
+  auto* owner = &preprocessor;
+  auto location = loc;
+  auto resume = [=]() -> std::optional<PreprocessingState> {
     if (d->pragmaOnceProtectedFiles_.contains(fileName)) return std::nullopt;

     if (auto it = d->ifndefProtectedFiles_.find(fileName);
@@ -4099,10 +4103,10 @@ void PendingInclude::resolveWith(std::optional<std::string> resolvedFileName,
     auto sourceFile = d->findSourceFile(fileName);
     if (!sourceFile) {
       PendingFileContent request{
-          .preprocessor = preprocessor,
+          .preprocessor = *owner,
           .fileName = fileName,
           .isSystemHeader = isSystemHeader,
-          .loc = loc,
+          .loc = location,
       };
       return request;
     }
