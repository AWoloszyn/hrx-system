diff --git a/packages/cxx-gen-ast/src/semanticClassification.ts b/packages/cxx-gen-ast/src/semanticClassification.ts
--- a/packages/cxx-gen-ast/src/semanticClassification.ts
+++ b/packages/cxx-gen-ast/src/semanticClassification.ts
@@ -118,6 +118,12 @@
   },

   "::cxx::ClassSymbol": {
+    minimumAlignmentShift_: {
+      cls: "P",
+      read: "$->minimumAlignment()",
+      write: "$->setMinimumAlignment($value)",
+      why: "the accessor represents the encoded request as byte alignment",
+    },
     flags_: { cls: "D", why: "the individual bit-fields are persisted" },
     baseClasses_: {
       cls: "P",
diff --git a/src/parser/cxx/archive.h b/src/parser/cxx/archive.h
--- a/src/parser/cxx/archive.h
+++ b/src/parser/cxx/archive.h
@@ -70,7 +70,7 @@
 class ArchiveWriter {
  public:
   static constexpr std::string_view kMagic = "CXXARCH";
-  static constexpr std::uint32_t kSchemaMajor = 5;
+  static constexpr std::uint32_t kSchemaMajor = 6;
   static constexpr std::uint32_t kSchemaMinor = 0;

   void setEnvelope(ArchiveEnvelope envelope) {
diff --git a/src/parser/cxx/ast_rewriter_specifiers.cc b/src/parser/cxx/ast_rewriter_specifiers.cc
--- a/src/parser/cxx/ast_rewriter_specifiers.cc
+++ b/src/parser/cxx/ast_rewriter_specifiers.cc
@@ -1066,6 +1066,8 @@
   }
   classSymbol->setPackAlignment(ast->symbol->packAlignment());
   classSymbol->setPacked(ast->symbol->isPacked());
+  classSymbol->setMinimumAlignment(ast->symbol->minimumAlignment());
+  binder()->applyLayoutAttributes(classSymbol, copy->attributeList);
   classSymbol->setAccessControlDisabled(ast->symbol->isAccessControlDisabled());
   binder()->inheritDeclarationAttributes(classSymbol, ast->symbol);
   classSymbol->setDeclaration(copy);
diff --git a/src/parser/cxx/binder.cc b/src/parser/cxx/binder.cc
--- a/src/parser/cxx/binder.cc
+++ b/src/parser/cxx/binder.cc
@@ -2989,7 +2989,7 @@
   if (!checkExplicitAlignment(*requested, field->type(), decl.location()))
     return;

-  field->setExplicitAlignment(*requested);
+  field->setExplicitAlignment(std::max(field->explicitAlignment(), *requested));
 }

 void Binder::applyExplicitAlignment(VariableSymbol* variable,
@@ -3000,7 +3000,8 @@
   if (!checkExplicitAlignment(*requested, variable->type(), decl.location()))
     return;

-  variable->setExplicitAlignment(*requested);
+  variable->setExplicitAlignment(
+      std::max(variable->explicitAlignment(), *requested));
 }

 auto Binder::declareField(DeclaratorAST* declarator, const Decl& decl)
diff --git a/src/parser/cxx/binder_complete_class.cc b/src/parser/cxx/binder_complete_class.cc
--- a/src/parser/cxx/binder_complete_class.cc
+++ b/src/parser/cxx/binder_complete_class.cc
@@ -41,6 +41,7 @@
 #include <algorithm>
 #include <format>
 #include <functional>
+#include <limits>
 #include <unordered_map>
 #include <unordered_set>

@@ -2241,13 +2242,13 @@
   const MemoryLayout* memoryLayout;
   std::unique_ptr<ClassLayout> layout;

-  int calculatedSize = 0;
+  std::int64_t calculatedSize = 0;
   int calculatedAlignment = 1;
   std::uint64_t emittedEnd = 0;
   std::uint32_t currentIndex = 0;

-  int nextBitPos = 0;
-  int runStartByte = 0;
+  std::int64_t nextBitPos = 0;
+  std::int64_t runStartByte = 0;
   std::uint32_t runIndex = 0;
   bool inBitfieldRun = false;
   std::vector<FieldSymbol*> runFields;
@@ -2291,7 +2292,7 @@
   void closeBitfieldRun();
   void propagateBaseFields();
   void propagateAnonymousFields(ClassSymbol* cls, std::uint64_t baseOffset);
-  void finalize();
+  auto finalize() -> std::expected<bool, std::string>;
   void buildVTableLayout();
 };

@@ -2320,9 +2321,7 @@
   layoutVirtualBases();

   propagateBaseFields();
-  finalize();
-
-  return true;
+  return finalize();
 }

 auto Binder::BuildRecordLayout::validate() -> std::expected<bool, std::string> {
@@ -2659,8 +2658,9 @@
     }

     if (!isEmpty) {
-      calculatedSize = std::max(calculatedSize,
-                                static_cast<int>(baseOffset) + baseSizeInBytes);
+      calculatedSize = std::max<std::int64_t>(
+          calculatedSize,
+          static_cast<std::int64_t>(baseOffset) + baseSizeInBytes);
       emittedEnd = std::max(emittedEnd, baseOffset + baseSizeInBytes);
     }
     recordNonVirtualClassSubobjects(baseClassSymbol, baseOffset);
@@ -2799,8 +2799,9 @@
     layout->addVirtualBase(baseClassSymbol);

     if (!isEmpty) {
-      calculatedSize = std::max(calculatedSize,
-                                static_cast<int>(baseOffset) + baseSizeInBytes);
+      calculatedSize = std::max<std::int64_t>(
+          calculatedSize,
+          static_cast<std::int64_t>(baseOffset) + baseSizeInBytes);
       emittedEnd = std::max(emittedEnd, baseOffset + baseSizeInBytes);
     }
     recordNonVirtualClassSubobjects(baseClassSymbol, baseOffset);
@@ -2858,7 +2859,9 @@
     }
     if (!isUnion && fieldSizeBits > 0) {
       // Zero-width fields impose their type's boundary even under pragma pack.
-      auto alignBits = field->alignment() * 8;
+      auto alignBits = std::uint64_t(std::max(field->alignment(),
+                                              field->explicitAlignment())) *
+                       8;
       nextBitPos = align_to(nextBitPos, alignBits);
       calculatedSize = (nextBitPos + 7) / 8;
     }
@@ -2880,9 +2883,17 @@
     // The bit payload determines occupied storage. Natural or pragma-capped
     // record alignment supplies any tail padding during finalization.
     auto fieldSizeForUnion = (bitWidth + 7) / 8;
-    calculatedSize = std::max(calculatedSize, fieldSizeForUnion);
+    calculatedSize = std::max<std::int64_t>(calculatedSize, fieldSizeForUnion);
     calculatedAlignment = std::max(calculatedAlignment, fieldAlign);
     return true;
+  }
+
+  // An explicit request begins a new aligned allocation unit. A pragma cap
+  // still limits that request, unlike a zero-width boundary above.
+  if (field->explicitAlignment()) {
+    closeBitfieldRun();
+    nextBitPos = align_to(nextBitPos, std::uint64_t(fieldAlign) * 8);
+    calculatedSize = nextBitPos / 8;
   }

   // A pragma cap permits consecutive bitfields to cross their declared
@@ -2955,9 +2966,13 @@
                     to_string(field->type(), field->name())));
   }

+  if (size.value() > std::numeric_limits<int>::max()) {
+    return std::unexpected("record layout exceeds the supported size range");
+  }
+
   if (isUnion) {
     field->setLocalOffset(0);
-    calculatedSize = std::max(calculatedSize, int(size.value()));
+    calculatedSize = std::max<std::int64_t>(calculatedSize, int(size.value()));

     ClassLayout::MemberInfo fieldInfo;
     fieldInfo.offset = 0;
@@ -2973,6 +2988,9 @@
       fieldOffset = classSubobjectOffset(fieldClass->symbol(), tryZero,
                                          std::max(fieldAlign, 1));
     }
+    if (fieldOffset + size.value() > std::numeric_limits<int>::max()) {
+      return std::unexpected("record layout exceeds the supported size range");
+    }
     field->setLocalOffset(static_cast<int>(fieldOffset));

     padTo(fieldOffset);
@@ -2982,8 +3000,8 @@
     fieldInfo.index = currentIndex++;
     layout->setFieldInfo(field, fieldInfo);

-    calculatedSize =
-        std::max(calculatedSize, static_cast<int>(fieldOffset + size.value()));
+    calculatedSize = std::max<std::int64_t>(
+        calculatedSize, static_cast<std::int64_t>(fieldOffset + size.value()));
     emittedEnd = std::max(emittedEnd, fieldOffset + size.value());
     if (fieldClass && fieldClass->symbol())
       recordNonVirtualClassSubobjects(fieldClass->symbol(), fieldOffset);
@@ -3121,7 +3139,7 @@
   emittedEnd = offset;
 }

-void Binder::BuildRecordLayout::finalize() {
+auto Binder::BuildRecordLayout::finalize() -> std::expected<bool, std::string> {
   if (auto requested = classSymbol->explicitAlignment()) {
     if (requested < calculatedAlignment) {
       binder.error(
@@ -3134,11 +3152,16 @@
     }
   }

+  calculatedAlignment =
+      std::max(calculatedAlignment, classSymbol->minimumAlignment());
   const auto dataSize = static_cast<std::uint64_t>(calculatedSize);

   if (calculatedSize == 0) calculatedSize = 1;

   calculatedSize = align_to(calculatedSize, calculatedAlignment);
+  if (calculatedSize > std::numeric_limits<int>::max()) {
+    return std::unexpected("record layout exceeds the supported size range");
+  }

   padTo(static_cast<std::uint64_t>(calculatedSize));

@@ -3152,6 +3175,7 @@
   classSymbol->setLayout(std::move(layout));

   buildVTableLayout();
+  return true;
 }

 void Binder::BuildRecordLayout::buildVTableLayout() {
diff --git a/src/parser/cxx/binder_layout_attributes.cc b/src/parser/cxx/binder_layout_attributes.cc
--- a/src/parser/cxx/binder_layout_attributes.cc
+++ b/src/parser/cxx/binder_layout_attributes.cc
@@ -19,10 +19,15 @@
 // SOFTWARE.

 #include <cxx/ast.h>
+#include <cxx/ast_interpreter.h>
 #include <cxx/attributes.h>
 #include <cxx/binder.h>
 #include <cxx/decl.h>
+#include <cxx/dependent_types.h>
 #include <cxx/symbols.h>
+
+#include <algorithm>
+#include <limits>

 namespace cxx {

@@ -32,9 +37,71 @@
   static constexpr AttributeSpelling spellings[] = {
       {AttributeSyntax::kGnu, "", "packed"},
       {AttributeSyntax::kCxx, "gnu", "packed"},
+      {AttributeSyntax::kGnu, "", "aligned"},
+      {AttributeSyntax::kCxx, "gnu", "aligned"},
   };
   visitAttributesBySpelling(
       unit_, attributes, spellings, [&](AttributeRef attribute) {
+        if (attribute.spelling->name == "aligned") {
+          auto record = symbol_cast<ClassSymbol>(symbol);
+          auto field = symbol_cast<FieldSymbol>(symbol);
+          auto variable = symbol_cast<VariableSymbol>(symbol);
+          if (!record && !(field && !field->isStatic()) && !variable) {
+            error(attribute.location,
+                  "'aligned' attribute requires a record, non-static data "
+                  "member, or variable");
+            return true;
+          }
+          if (!attribute.argumentClause) {
+            error(
+                attribute.location,
+                "'aligned' attribute requires an explicit alignment argument");
+            return true;
+          }
+          auto arguments = attribute.argumentClause->expressionList;
+          if (!arguments || arguments->next) {
+            error(attribute.location,
+                  "'aligned' attribute requires one argument");
+            return true;
+          }
+          if (isDependent(unit_, arguments->value)) return true;
+          ASTInterpreter interpreter{unit_};
+          auto value = interpreter.evaluate(arguments->value);
+          auto integer = value ? interpreter.toInt(*value) : std::nullopt;
+          if (!integer ||
+              !traits.is_integral_or_unscoped_enum(arguments->value->type)) {
+            error(attribute.location,
+                  "'aligned' attribute requires an integer constant");
+            return true;
+          }
+          if (*integer <= 0 || (*integer & (*integer - 1)) != 0) {
+            error(attribute.location,
+                  "requested alignment is not a positive power of 2");
+            return true;
+          }
+          if (*integer > std::numeric_limits<int>::max()) {
+            error(attribute.location,
+                  "requested alignment exceeds the supported layout range");
+            return true;
+          }
+          auto alignment = static_cast<int>(*integer);
+          if (record) {
+            if (record->isComplete() && alignment > record->alignment()) {
+              error(attribute.location,
+                    "'aligned' attribute must precede record completion");
+              return true;
+            }
+            record->setMinimumAlignment(
+                std::max(record->minimumAlignment(), alignment));
+          } else if (field) {
+            field->setExplicitAlignment(
+                std::max(field->explicitAlignment(), alignment));
+          } else {
+            variable->setExplicitAlignment(
+                std::max(variable->explicitAlignment(), alignment));
+          }
+          return true;
+        }
         if (attribute.argumentClause) {
           error(attribute.location, "'packed' attribute takes no arguments");
           return true;
diff --git a/src/parser/cxx/parser.cc b/src/parser/cxx/parser.cc
--- a/src/parser/cxx/parser.cc
+++ b/src/parser/cxx/parser.cc
@@ -8925,8 +8925,7 @@
   List<AttributeSpecifierAST*> attributes{specifier};
   visitAttributesBySpelling(
       unit_, &attributes, spellings, [&](AttributeRef attribute) {
-        if (attribute.spelling->name == "aligned" ||
-            layoutContext == LayoutAttributeContext::kOther) {
+        if (layoutContext == LayoutAttributeContext::kOther) {
           unit_->error(
               attribute.location,
               std::format(
diff --git a/src/parser/cxx/private/semantic_model.h b/src/parser/cxx/private/semantic_model.h
--- a/src/parser/cxx/private/semantic_model.h
+++ b/src/parser/cxx/private/semantic_model.h
@@ -147,6 +147,9 @@
     {"::cxx::ClassSymbol", "packAlignment_", FieldPersistence::kPersisted, ""},
     {"::cxx::ClassSymbol", "flags_", FieldPersistence::kDerived,
      "the individual bit-fields are persisted"},
+    {"::cxx::ClassSymbol", "minimumAlignmentShift_",
+     FieldPersistence::kPersisted,
+     "the accessor represents the encoded request as byte alignment"},
     {"::cxx::ClassSymbol", "isPacked_", FieldPersistence::kPersisted, ""},
     {"::cxx::ClassSymbol", "isUnion_", FieldPersistence::kPersisted, ""},
     {"::cxx::ClassSymbol", "isFinal_", FieldPersistence::kPersisted, ""},
diff --git a/src/parser/cxx/semantic_codec.cc b/src/parser/cxx/semantic_codec.cc
--- a/src/parser/cxx/semantic_codec.cc
+++ b/src/parser/cxx/semantic_codec.cc
@@ -2234,6 +2234,8 @@
   out.varI32(static_cast<std::int32_t>(self->explicitAlignment()));
   // ::cxx::ClassSymbol::packAlignment_
   out.varI32(static_cast<std::int32_t>(self->packAlignment()));
+  // ::cxx::ClassSymbol::minimumAlignmentShift_
+  out.varU32(static_cast<std::uint32_t>(self->minimumAlignment()));
   // ::cxx::ClassSymbol::isPacked_
   out.varU32(static_cast<std::uint32_t>(self->isPacked()));
   // ::cxx::ClassSymbol::isUnion_
@@ -10367,42 +10369,45 @@
   // ::cxx::ClassSymbol::packAlignment_
   int value83 = static_cast<int>(in.varI32());
   self->setPackAlignment(std::move(value83));
+  // ::cxx::ClassSymbol::minimumAlignmentShift_
+  std::uint32_t value84 = static_cast<std::uint32_t>(in.varU32());
+  self->setMinimumAlignment(std::move(value84));
   // ::cxx::ClassSymbol::isPacked_
-  std::uint32_t value84 = static_cast<std::uint32_t>(in.varU32());
-  self->setPacked(std::move(value84));
+  std::uint32_t value85 = static_cast<std::uint32_t>(in.varU32());
+  self->setPacked(std::move(value85));
   // ::cxx::ClassSymbol::isUnion_
-  unsigned int value85 = static_cast<unsigned int>(in.varU32());
-  self->setIsUnion(std::move(value85));
+  unsigned int value86 = static_cast<unsigned int>(in.varU32());
+  self->setIsUnion(std::move(value86));
   // ::cxx::ClassSymbol::isFinal_
-  unsigned int value86 = static_cast<unsigned int>(in.varU32());
-  self->setFinal(std::move(value86));
+  unsigned int value87 = static_cast<unsigned int>(in.varU32());
+  self->setFinal(std::move(value87));
   // ::cxx::ClassSymbol::isComplete_
-  unsigned int value87 = static_cast<unsigned int>(in.varU32());
-  self->setComplete(std::move(value87));
+  unsigned int value88 = static_cast<unsigned int>(in.varU32());
+  self->setComplete(std::move(value88));
   // ::cxx::ClassSymbol::isFriend_
-  unsigned int value88 = static_cast<unsigned int>(in.varU32());
-  self->setFriend(std::move(value88));
+  unsigned int value89 = static_cast<unsigned int>(in.varU32());
+  self->setFriend(std::move(value89));
   // ::cxx::ClassSymbol::isAccessControlDisabled_
-  unsigned int value89 = static_cast<unsigned int>(in.varU32());
-  self->setAccessControlDisabled(std::move(value89));
+  unsigned int value90 = static_cast<unsigned int>(in.varU32());
+  self->setAccessControlDisabled(std::move(value90));
   // ::cxx::ClassSymbol::isPolymorphic_
-  unsigned int value90 = static_cast<unsigned int>(in.varU32());
-  self->setPolymorphic(std::move(value90));
+  unsigned int value91 = static_cast<unsigned int>(in.varU32());
+  self->setPolymorphic(std::move(value91));
   // ::cxx::ClassSymbol::isAbstract_
-  unsigned int value91 = static_cast<unsigned int>(in.varU32());
-  self->setAbstract(std::move(value91));
+  unsigned int value92 = static_cast<unsigned int>(in.varU32());
+  self->setAbstract(std::move(value92));
   // ::cxx::ClassSymbol::hasVirtualDestructor_
-  unsigned int value92 = static_cast<unsigned int>(in.varU32());
-  self->setHasVirtualDestructor(std::move(value92));
+  unsigned int value93 = static_cast<unsigned int>(in.varU32());
+  self->setHasVirtualDestructor(std::move(value93));
   // ::cxx::ClassSymbol::isClosureType_
-  unsigned int value93 = static_cast<unsigned int>(in.varU32());
-  self->setIsClosureType(std::move(value93));
+  unsigned int value94 = static_cast<unsigned int>(in.varU32());
+  self->setIsClosureType(std::move(value94));
   // ::cxx::ClassSymbol::hasLambdaCapture_
-  unsigned int value94 = static_cast<unsigned int>(in.varU32());
-  self->setHasLambdaCapture(std::move(value94));
+  unsigned int value95 = static_cast<unsigned int>(in.varU32());
+  self->setHasLambdaCapture(std::move(value95));
   // ::cxx::ClassSymbol::hasUserDeclaredConstructors_
-  unsigned int value95 = static_cast<unsigned int>(in.varU32());
-  self->setHasUserDeclaredConstructors(std::move(value95));
+  unsigned int value96 = static_cast<unsigned int>(in.varU32());
+  self->setHasUserDeclaredConstructors(std::move(value96));
 }

 void SemanticDecoder::readSymbolEnumSymbol(
diff --git a/src/parser/cxx/symbols.h b/src/parser/cxx/symbols.h
--- a/src/parser/cxx/symbols.h
+++ b/src/parser/cxx/symbols.h
@@ -1166,6 +1166,15 @@
   [[nodiscard]] auto instantiationPattern() const -> ClassSymbol*;
   void setInstantiationPattern(ClassSymbol* instantiationPattern);

+  // GNU minimum record alignment in bytes, independently of standard alignas.
+  [[nodiscard]] auto minimumAlignment() const -> int {
+    return minimumAlignmentShift_ ? 1u << (minimumAlignmentShift_ - 1) : 0;
+  }
+  void setMinimumAlignment(int alignment) {
+    minimumAlignmentShift_ =
+        alignment ? std::countr_zero(unsigned(alignment)) + 1 : 0;
+  }
+
   // GNU packing reduces natural member alignment independently of pragma caps.
   [[nodiscard]] auto isPacked() const -> bool { return isPacked_; }
   void setPacked(bool packed) { isPacked_ = packed; }
@@ -1198,6 +1207,9 @@
   union {
     std::uint32_t flags_{};
     struct {
+      // Encoded minimum record alignment: zero means unspecified, otherwise
+      // log2 + 1.
+      std::uint32_t minimumAlignmentShift_ : 6;
       // Whether GNU packed applies to this declaration.
       std::uint32_t isPacked_ : 1;
       std::uint32_t isUnion_ : 1;
diff --git a/src/parser/cxx/util.cc b/src/parser/cxx/util.cc
--- a/src/parser/cxx/util.cc
+++ b/src/parser/cxx/util.cc
@@ -22,7 +22,7 @@

 namespace cxx {

-auto align_to(int n, int align) -> int {
+auto align_to(std::uint64_t n, std::uint64_t align) -> std::uint64_t {
   return (n + align - 1) / align * align;
 }

diff --git a/src/parser/cxx/util.h b/src/parser/cxx/util.h
--- a/src/parser/cxx/util.h
+++ b/src/parser/cxx/util.h
@@ -20,11 +20,12 @@

 #pragma once

+#include <cstdint>
 #include <functional>

 namespace cxx {

-auto align_to(int n, int align) -> int;
+auto align_to(std::uint64_t n, std::uint64_t align) -> std::uint64_t;

 template <typename T>
 void hash_combine(std::size_t& seed, const T& val) {
diff --git a/tests/unit_tests/sema/aligned_layout_01.cc b/tests/unit_tests/sema/aligned_layout_01.cc
new file mode 100644
--- /dev/null
+++ b/tests/unit_tests/sema/aligned_layout_01.cc
@@ -0,0 +1,100 @@
+// Copyright 2026 The IREE Authors
+//
+// Licensed under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+
+struct __attribute__((packed, aligned(8))) AlignedPacket {
+  unsigned char tag;
+  unsigned value;
+};
+static_assert(sizeof(AlignedPacket) == 8);
+static_assert(alignof(AlignedPacket) == 8);
+static_assert(__builtin_offsetof(AlignedPacket, value) == 1);
+
+struct [[gnu::packed]] AlignedField {
+  unsigned char tag;
+  unsigned value __attribute__((aligned(2)));
+};
+static_assert(sizeof(AlignedField) == 6);
+static_assert(alignof(AlignedField) == 2);
+static_assert(__builtin_offsetof(AlignedField, value) == 2);
+
+struct AlignedAliases {
+  unsigned char tag;
+  unsigned value __attribute__((unused, __aligned__(16)));
+  [[using gnu: aligned(8)]] unsigned tail;
+};
+static_assert(sizeof(AlignedAliases) == 32);
+static_assert(__builtin_offsetof(AlignedAliases, value) == 16);
+static_assert(__builtin_offsetof(AlignedAliases, tail) == 24);
+
+struct [[gnu::aligned(2)]] NaturalAlignment {
+  unsigned value;
+};
+static_assert(alignof(NaturalAlignment) == alignof(unsigned));
+
+struct [[gnu::aligned(8), gnu::aligned(16)]] RepeatedAlignment {
+  alignas(8) unsigned first __attribute__((aligned(32), aligned(16)));
+};
+static_assert(sizeof(RepeatedAlignment) == 32);
+static_assert(alignof(RepeatedAlignment) == 32);
+
+#pragma pack(push, 1)
+struct CappedAlignment {
+  unsigned char tag;
+  unsigned value __attribute__((aligned(16)));
+} __attribute__((aligned(8)));
+#pragma pack(pop)
+static_assert(sizeof(CappedAlignment) == 8);
+static_assert(alignof(CappedAlignment) == 8);
+static_assert(__builtin_offsetof(CappedAlignment, value) == 1);
+
+template <unsigned Alignment>
+struct AlignedBlock {
+  unsigned char tag;
+  unsigned value __attribute__((aligned(Alignment)));
+} __attribute__((packed, aligned(Alignment * 2)));
+static_assert(sizeof(AlignedBlock<8>) == 16);
+static_assert(alignof(AlignedBlock<8>) == 16);
+static_assert(__builtin_offsetof(AlignedBlock<8>, value) == 8);
+static_assert(sizeof(AlignedBlock<2>) == 8);
+static_assert(alignof(AlignedBlock<2>) == 4);
+static_assert(__builtin_offsetof(AlignedBlock<2>, value) == 2);
+static_assert(sizeof(AlignedBlock<8>) == 16);
+
+struct __attribute__((aligned(16))) ForwardAlignment;
+struct ForwardAlignment {
+  unsigned value;
+};
+static_assert(sizeof(ForwardAlignment) == 16);
+static_assert(alignof(ForwardAlignment) == 16);
+
+struct __attribute__((packed)) AlignedPackedBits {
+  char tag;
+  __attribute__((aligned(2))) unsigned bits : 3;
+  char tail;
+};
+static_assert(sizeof(AlignedPackedBits) == 4 &&
+              alignof(AlignedPackedBits) == 2 &&
+              __builtin_offsetof(AlignedPackedBits, tail) == 3);
+struct AlignedBits {
+  char tag;
+  __attribute__((aligned(16))) unsigned bits : 3;
+  char tail;
+};
+static_assert(sizeof(AlignedBits) == 32 && alignof(AlignedBits) == 16 &&
+              __builtin_offsetof(AlignedBits, tail) == 17);
+struct __attribute__((packed)) AlignedZeroWidth {
+  char tag;
+  __attribute__((aligned(8))) unsigned : 0;
+  char tail;
+};
+static_assert(sizeof(AlignedZeroWidth) == 9 && alignof(AlignedZeroWidth) == 1 &&
+              __builtin_offsetof(AlignedZeroWidth, tail) == 8);
+
+enum AlignmentValue { AlignmentBytes = 8 };
+struct [[gnu::aligned(AlignmentBytes)]] EnumAligned {
+  unsigned value;
+};
+static_assert(alignof(EnumAligned) == 8);
