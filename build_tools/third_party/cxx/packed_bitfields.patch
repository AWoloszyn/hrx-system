diff --git a/src/parser/cxx/binder_complete_class.cc b/src/parser/cxx/binder_complete_class.cc
--- a/src/parser/cxx/binder_complete_class.cc
+++ b/src/parser/cxx/binder_complete_class.cc
@@ -2827,6 +2827,9 @@

 auto Binder::BuildRecordLayout::layoutBitfield(FieldSymbol* field)
     -> std::expected<bool, std::string> {
+  if (memoryLayout->usesMicrosoftBitfieldLayout()) {
+    return std::unexpected("Microsoft bitfield layout is not supported");
+  }
   const bool isUnion = classSymbol->isUnion();

   int bitWidth = 0;
@@ -2847,7 +2850,8 @@
       closeBitfieldRun();
     }
     if (!isUnion && fieldSizeBits > 0) {
-      auto alignBits = fieldAlign * 8;
+      // Zero-width fields impose their type's boundary even under pragma pack.
+      auto alignBits = field->alignment() * 8;
       nextBitPos = align_to(nextBitPos, alignBits);
       calculatedSize = (nextBitPos + 7) / 8;
     }
@@ -2866,13 +2870,17 @@
     fieldInfo.allocUnitSizeBytes = (bitWidth + 7) / 8;
     layout->setFieldInfo(field, fieldInfo);

-    auto fieldSizeForUnion = std::max(fieldSizeBytes, (bitWidth + 7) / 8);
+    // The bit payload determines occupied storage. Natural or pragma-capped
+    // record alignment supplies any tail padding during finalization.
+    auto fieldSizeForUnion = (bitWidth + 7) / 8;
     calculatedSize = std::max(calculatedSize, fieldSizeForUnion);
     calculatedAlignment = std::max(calculatedAlignment, fieldAlign);
     return true;
   }

-  if (fieldSizeBits > 0) {
+  // A pragma cap permits consecutive bitfields to cross their declared
+  // type's allocation-unit boundary, even when the cap equals its alignment.
+  if (packValue == 0 && fieldSizeBits > 0) {
     auto startUnit = nextBitPos / fieldSizeBits;
     auto endUnit = (nextBitPos + bitWidth - 1) / fieldSizeBits;
     if (startUnit != endUnit) {
diff --git a/src/parser/cxx/memory_layout.h b/src/parser/cxx/memory_layout.h
--- a/src/parser/cxx/memory_layout.h
+++ b/src/parser/cxx/memory_layout.h
@@ -67,6 +67,8 @@
   [[nodiscard]] auto arch() const -> std::string_view;
   [[nodiscard]] auto isWebAssembly() const -> bool;
   [[nodiscard]] auto usesArmMemberPointerAbi() const -> bool;
+  // Selects the source ABI independently of integer and pointer widths.
+  [[nodiscard]] auto usesMicrosoftBitfieldLayout() const -> bool;
   [[nodiscard]] auto defaultNewAlignment() const -> std::size_t;
   [[nodiscard]] auto maxAtomicInlineWidth() const -> std::size_t;
   [[nodiscard]] auto maxAtomicPromoteWidth() const -> std::size_t;
diff --git a/src/parser/cxx/memory_layout.cc b/src/parser/cxx/memory_layout.cc
--- a/src/parser/cxx/memory_layout.cc
+++ b/src/parser/cxx/memory_layout.cc
@@ -500,6 +500,12 @@
          arch.starts_with("thumb") || isWebAssembly();
 }

+auto MemoryLayout::usesMicrosoftBitfieldLayout() const -> bool {
+  return triple_.find("-windows") != std::string::npos ||
+         triple_.find("-win32") != std::string::npos ||
+         triple_.find("-mingw") != std::string::npos;
+}
+
 auto MemoryLayout::defaultNewAlignment() const -> std::size_t { return 16; }

 auto MemoryLayout::maxAtomicInlineWidth() const -> std::size_t {
diff --git a/tests/unit_tests/sema/pragma_pack_bitfields_01.cc b/tests/unit_tests/sema/pragma_pack_bitfields_01.cc
--- /dev/null
+++ b/tests/unit_tests/sema/pragma_pack_bitfields_01.cc
@@ -0,0 +1,24 @@
+// RUN: %cxx -verify -fsyntax-only %s
+#pragma pack(push, 1)
+struct CrossingBits {
+  unsigned first : 31;
+  unsigned second : 2;
+};
+struct BitBoundary {
+  unsigned char first;
+  unsigned : 0;
+  unsigned char last;
+};
+union BitPayload {
+  unsigned bits : 3;
+};
+#pragma pack(pop)
+static_assert(sizeof(CrossingBits) == 5 && alignof(CrossingBits) == 1);
+static_assert(sizeof(BitBoundary) == 5 && alignof(BitBoundary) == 1);
+static_assert(__builtin_offsetof(BitBoundary, last) == 4);
+static_assert(sizeof(BitPayload) == 1 && alignof(BitPayload) == 1);
+
+union NaturalBitPayload {
+  unsigned bits : 3;
+};
+static_assert(sizeof(NaturalBitPayload) == 4 && alignof(NaturalBitPayload) == 4);
