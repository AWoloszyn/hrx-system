diff --git a/src/parser/cxx/archive.h b/src/parser/cxx/archive.h
--- a/src/parser/cxx/archive.h
+++ b/src/parser/cxx/archive.h
@@ -70,7 +70,7 @@
 class ArchiveWriter {
  public:
   static constexpr std::string_view kMagic = "CXXARCH";
-  static constexpr std::uint32_t kSchemaMajor = 9;
+  static constexpr std::uint32_t kSchemaMajor = 10;
   static constexpr std::uint32_t kSchemaMinor = 0;

   void setEnvelope(ArchiveEnvelope envelope) {
diff --git a/src/parser/cxx/ast.cc b/src/parser/cxx/ast.cc
--- a/src/parser/cxx/ast.cc
+++ b/src/parser/cxx/ast.cc
@@ -6980,6 +6980,7 @@
   if (elseStatement) node->elseStatement = elseStatement->clone(arena);

   node->symbol = symbol;
+  node->constexprValue = constexprValue;

   return node;
 }
@@ -6995,7 +6996,8 @@
                             SourceLocation lparenLoc, StatementAST* initializer,
                             ExpressionAST* condition, SourceLocation rparenLoc,
                             StatementAST* statement, SourceLocation elseLoc,
-                            StatementAST* elseStatement, BlockSymbol* symbol)
+                            StatementAST* elseStatement, BlockSymbol* symbol,
+                            std::optional<bool> constexprValue)
     -> IfStatementAST* {
   auto node = new (arena) IfStatementAST();
   node->attributeList = attributeList;
@@ -7009,6 +7011,7 @@
   node->elseLoc = elseLoc;
   node->elseStatement = elseStatement;
   node->symbol = symbol;
+  node->constexprValue = constexprValue;
   return node;
 }

@@ -7016,7 +7019,8 @@
                             List<AttributeSpecifierAST*>* attributeList,
                             StatementAST* initializer, ExpressionAST* condition,
                             StatementAST* statement,
-                            StatementAST* elseStatement, BlockSymbol* symbol)
+                            StatementAST* elseStatement, BlockSymbol* symbol,
+                            std::optional<bool> constexprValue)
     -> IfStatementAST* {
   auto node = new (arena) IfStatementAST();
   node->attributeList = attributeList;
@@ -7025,6 +7029,7 @@
   node->statement = statement;
   node->elseStatement = elseStatement;
   node->symbol = symbol;
+  node->constexprValue = constexprValue;
   return node;
 }

diff --git a/src/parser/cxx/ast.h b/src/parser/cxx/ast.h
--- a/src/parser/cxx/ast.h
+++ b/src/parser/cxx/ast.h
@@ -2378,6 +2378,9 @@
   SourceLocation elseLoc;
   StatementAST* elseStatement = nullptr;
   BlockSymbol* symbol = nullptr;
+  // Selected arm of a checked nondependent constexpr condition. An empty
+  // value denotes an ordinary if, a dependent condition, or diagnosed input.
+  std::optional<bool> constexprValue;

   void accept(ASTVisitor* visitor) override { visitor->visit(this); }

@@ -2394,7 +2397,8 @@
       SourceLocation lparenLoc, StatementAST* initializer,
       ExpressionAST* condition, SourceLocation rparenLoc,
       StatementAST* statement, SourceLocation elseLoc,
-      StatementAST* elseStatement, BlockSymbol* symbol) -> IfStatementAST*;
+      StatementAST* elseStatement, BlockSymbol* symbol,
+      std::optional<bool> constexprValue) -> IfStatementAST*;

   [[nodiscard]] static auto create(Arena* arena,
                                    List<AttributeSpecifierAST*>* attributeList,
@@ -2402,7 +2406,9 @@
                                    ExpressionAST* condition,
                                    StatementAST* statement,
                                    StatementAST* elseStatement,
-                                   BlockSymbol* symbol) -> IfStatementAST*;
+                                   BlockSymbol* symbol,
+                                   std::optional<bool> constexprValue)
+      -> IfStatementAST*;

  protected:
   IfStatementAST() : StatementAST(Kind) {}
diff --git a/src/parser/cxx/ast_interpreter_statements.cc b/src/parser/cxx/ast_interpreter_statements.cc
--- a/src/parser/cxx/ast_interpreter_statements.cc
+++ b/src/parser/cxx/ast_interpreter_statements.cc
@@ -167,6 +167,10 @@
   auto mark = interp.beginAutomaticScope();
   auto result = [&]() -> StatementResult {
     (void)interp.statement(ast->initializer);
+    if (interp.aborted()) return {};
+    if (ast->constexprValue.has_value())
+      return interp.statement(*ast->constexprValue ? ast->statement
+                                                  : ast->elseStatement);
     auto conditionResult = interp.expression(ast->condition);

     if (conditionResult.has_value()) {
diff --git a/src/parser/cxx/ast_rewriter_statements.cc b/src/parser/cxx/ast_rewriter_statements.cc
--- a/src/parser/cxx/ast_rewriter_statements.cc
+++ b/src/parser/cxx/ast_rewriter_statements.cc
@@ -422,21 +422,13 @@
   copy->lparenLoc = ast->lparenLoc;
   copy->initializer = rewrite.statement(ast->initializer);
   copy->condition = rewrite.expression(ast->condition);
-  typeChecker().check_bool_condition(copy->condition);
+  copy->constexprValue = ast->constexprValue;
+  typeChecker().check_if_statement(copy);
   copy->rparenLoc = ast->rparenLoc;

-  std::optional<bool> constexprValue;
-  if (ast->constexprLoc && copy->condition && !binder()->inTemplate() &&
-      !isDependent(rewrite.unit_, copy->condition)) {
-    auto interp = ASTInterpreter{rewrite.unit_};
-    if (auto val = interp.evaluate(copy->condition)) {
-      constexprValue = interp.toBool(*val);
-    }
-  }
-
   copy->elseLoc = ast->elseLoc;
-  if (constexprValue.has_value()) {
-    if (*constexprValue) {
+  if (copy->constexprValue.has_value()) {
+    if (*copy->constexprValue) {
       copy->statement = rewrite.statement(ast->statement);
     } else {
       copy->elseStatement = rewrite.statement(ast->elseStatement);
diff --git a/src/parser/cxx/function_body_warnings.cc b/src/parser/cxx/function_body_warnings.cc
--- a/src/parser/cxx/function_body_warnings.cc
+++ b/src/parser/cxx/function_body_warnings.cc
@@ -99,7 +99,9 @@
   auto preVisit(AST*) -> bool override { return !found_; }

   void visit(IfStatementAST* ast) override {
-    if (auto taken = foldCondition(unit_, ast->condition)) {
+    if (auto taken = ast->constexprValue.has_value()
+                         ? ast->constexprValue
+                         : foldCondition(unit_, ast->condition)) {
       accept(*taken ? ast->statement : ast->elseStatement);
       return;
     }
@@ -208,7 +210,9 @@

     case ASTKind::IfStatement: {
       auto ast = static_cast<IfStatementAST*>(stmt);
-      if (auto value = conditionValue(ast->condition)) {
+      if (auto value = ast->constexprValue.has_value()
+                           ? ast->constexprValue
+                           : conditionValue(ast->condition)) {
         if (*value) return completesNormally(ast->statement);
         return completesNormally(ast->elseStatement);
       }
diff --git a/src/parser/cxx/parser.cc b/src/parser/cxx/parser.cc
--- a/src/parser/cxx/parser.cc
+++ b/src/parser/cxx/parser.cc
@@ -4516,7 +4516,10 @@
   parse_init_statement(ast->initializer);

   parse_condition(ast->condition, ExprContext{});
-  check_bool_condition(ast->condition);
+  TypeChecker check{unit_};
+  check.setScope(scope());
+  check.setReportErrors(config().checkTypes);
+  check.check_if_statement(ast);

   expect(TokenKind::T_RPAREN, ast->rparenLoc);

@@ -4540,12 +4543,8 @@
 auto Parser::discardedIfBranches(IfStatementAST* ast) -> DiscardedIfBranches {
   if (!ast->constexprLoc || !ast->condition) return {};

-  if (!isDependent(unit_, ast->condition)) {
-    auto interp = ASTInterpreter{unit_, binder_.scope()};
-    if (auto value = interp.evaluate(ast->condition)) {
-      if (auto taken = interp.toBool(*value)) return {!*taken, *taken};
-    }
-  }
+  if (ast->constexprValue.has_value())
+    return {!*ast->constexprValue, *ast->constexprValue};

   return {binder_.inTemplate(), binder_.inTemplate()};
 }
diff --git a/src/parser/cxx/private/semantic_model.h b/src/parser/cxx/private/semantic_model.h
--- a/src/parser/cxx/private/semantic_model.h
+++ b/src/parser/cxx/private/semantic_model.h
@@ -880,6 +880,8 @@
     {"::cxx::IfStatementAST", "elseStatement", FieldPersistence::kPersisted,
      ""},
     {"::cxx::IfStatementAST", "symbol", FieldPersistence::kPersisted, ""},
+    {"::cxx::IfStatementAST", "constexprValue", FieldPersistence::kPersisted,
+     ""},
     {"::cxx::ConstevalIfStatementAST", "attributeList",
      FieldPersistence::kPersisted, ""},
     {"::cxx::ConstevalIfStatementAST", "ifLoc", FieldPersistence::kPersisted,
diff --git a/src/parser/cxx/semantic_codec.cc b/src/parser/cxx/semantic_codec.cc
--- a/src/parser/cxx/semantic_codec.cc
+++ b/src/parser/cxx/semantic_codec.cc
@@ -4254,6 +4254,9 @@
   out.varU32(static_cast<std::uint32_t>(astRef(self->elseStatement)));
   // ::cxx::IfStatementAST::symbol
   out.varU32(static_cast<std::uint32_t>(symbolRef(self->symbol)));
+  // ::cxx::IfStatementAST::constexprValue
+  out.boolean(self->constexprValue.has_value());
+  if (self->constexprValue.has_value()) out.boolean(*self->constexprValue);
 }

 void SemanticEncoder::writeAstConstevalIfStatementAST(
@@ -13664,6 +13667,13 @@
   cxx::BlockSymbol* value11 =
       symbol_cast<BlockSymbol>(symbolAt(SymbolRef{in.varU32()}));
   self->symbol = std::move(value11);
+  // ::cxx::IfStatementAST::constexprValue
+  std::optional<bool> value12;
+  if (in.boolean()) {
+    bool value13 = in.boolean();
+    value12 = std::move(value13);
+  }
+  self->constexprValue = std::move(value12);
 }

 void SemanticDecoder::readAstConstevalIfStatementAST(
diff --git a/src/parser/cxx/type_checker.cc b/src/parser/cxx/type_checker.cc
--- a/src/parser/cxx/type_checker.cc
+++ b/src/parser/cxx/type_checker.cc
@@ -7747,6 +7747,19 @@
   return false;
 }

+void TypeChecker::check_if_statement(IfStatementAST* ast) {
+  if (!check_bool_condition(ast->condition) || !ast->constexprLoc ||
+      ast->constexprValue.has_value() || isDependent(unit_, ast->condition))
+    return;
+
+  auto interpreter = ASTInterpreter{unit_, scope_};
+  if (auto value = interpreter.evaluate(ast->condition))
+    ast->constexprValue = interpreter.toBool(*value);
+  if (!ast->constexprValue.has_value())
+    error(ast->condition->firstSourceLocation(),
+          "condition of 'if constexpr' is not a constant expression");
+}
+
 void TypeChecker::check_integral_condition(ExpressionAST*& expr) {
   if (!expr || !expr->type) return;
   if (isDependent(unit_, expr->type)) return;
diff --git a/src/parser/cxx/type_checker.h b/src/parser/cxx/type_checker.h
--- a/src/parser/cxx/type_checker.h
+++ b/src/parser/cxx/type_checker.h
@@ -82,6 +82,7 @@
                                         ScopeSymbol* functionScope);

   auto check_bool_condition(ExpressionAST*& ast) -> bool;
+  void check_if_statement(IfStatementAST* ast);
   void check_integral_condition(ExpressionAST*& ast);
   void check_init_declarator(InitDeclaratorAST* initDecl,
                              SpecifierAST* typeSpecifier);
