diff --git a/src/codegen/cxx/codegen/codegen_expressions.cc b/src/codegen/cxx/codegen/codegen_expressions.cc
--- a/src/codegen/cxx/codegen/codegen_expressions.cc
+++ b/src/codegen/cxx/codegen/codegen_expressions.cc
@@ -1763,38 +1763,11 @@

 auto Codegen::ExpressionVisitor::operator()(BuiltinOffsetofExpressionAST* ast)
     -> ExpressionResult {
-  if (ast->symbol) {
-    auto loc = ast->firstSourceLocation();
-    auto resultType = gen.convertType(ast->type);
-
-    auto classType = unqualified_cast<ClassType>(ast->typeId->type);
-    if (!classType) {
-      return {gen.emitTodoExpr(ast->firstSourceLocation(),
-                               "__builtin_offsetof requires a class type")};
-    }
-
-    auto classSymbol = classType->symbol();
-    auto layout = classSymbol->layout();
-    if (!layout) {
-      return {gen.emitTodoExpr(ast->firstSourceLocation(),
-                               "class layout not computed")};
-    }
-
-    auto fieldInfo = layout->getFieldInfo(ast->symbol);
-    if (!fieldInfo) {
-      return {gen.emitTodoExpr(ast->firstSourceLocation(),
-                               "field not found in layout")};
-    }
-
-    auto op = gen.emitter_.constantInt(loc, resultType, fieldInfo->offset);
-
-    return {op};
-  }
-
-  auto op =
-      gen.emitTodoExpr(ast->firstSourceLocation(), to_string(ast->kind()));
-
-  return {op};
+  auto loc = ast->firstSourceLocation();
+  if (!ast->value) {
+    return {gen.emitTodoExpr(loc, "offsetof has no resolved constant value")};
+  }
+  return {gen.emitter_.constantInt(loc, gen.convertType(ast->type), *ast->value)};
 }

 auto Codegen::ExpressionVisitor::operator()(TypeidExpressionAST* ast)
diff --git a/src/parser/cxx/archive.h b/src/parser/cxx/archive.h
--- a/src/parser/cxx/archive.h
+++ b/src/parser/cxx/archive.h
@@ -70,7 +70,7 @@
 class ArchiveWriter {
  public:
   static constexpr std::string_view kMagic = "CXXARCH";
-  static constexpr std::uint32_t kSchemaMajor = 8;
+  static constexpr std::uint32_t kSchemaMajor = 9;
   static constexpr std::uint32_t kSchemaMinor = 0;

   void setEnvelope(ArchiveEnvelope envelope) {
diff --git a/src/parser/cxx/ast.cc b/src/parser/cxx/ast.cc
--- a/src/parser/cxx/ast.cc
+++ b/src/parser/cxx/ast.cc
@@ -9497,7 +9497,9 @@
   node->offsetofLoc = offsetofLoc;
   node->lparenLoc = lparenLoc;

-  if (typeId) node->typeId = typeId->clone(arena);
+  if (typeId) {
+    node->typeId = typeId->clone(arena);
+  }

   node->commaLoc = commaLoc;
   node->identifierLoc = identifierLoc;
@@ -9513,6 +9515,7 @@
   node->rparenLoc = rparenLoc;
   node->identifier = identifier;
   node->symbol = symbol;
+  node->value = value;
   node->valueCategory = valueCategory;
   node->type = type;

@@ -9530,8 +9533,8 @@
     TypeIdAST* typeId, SourceLocation commaLoc, SourceLocation identifierLoc,
     List<DesignatorAST*>* designatorList, SourceLocation rparenLoc,
     const Identifier* identifier, FieldSymbol* symbol,
-    ValueCategory valueCategory, const Type* type)
-    -> BuiltinOffsetofExpressionAST* {
+    std::optional<std::uint64_t> value, ValueCategory valueCategory,
+    const Type* type) -> BuiltinOffsetofExpressionAST* {
   auto node = new (arena) BuiltinOffsetofExpressionAST();
   node->offsetofLoc = offsetofLoc;
   node->lparenLoc = lparenLoc;
@@ -9542,23 +9545,23 @@
   node->rparenLoc = rparenLoc;
   node->identifier = identifier;
   node->symbol = symbol;
-  node->valueCategory = valueCategory;
-  node->type = type;
-  return node;
-}
-
-auto BuiltinOffsetofExpressionAST::create(Arena* arena, TypeIdAST* typeId,
-                                          List<DesignatorAST*>* designatorList,
-                                          const Identifier* identifier,
-                                          FieldSymbol* symbol,
-                                          ValueCategory valueCategory,
-                                          const Type* type)
-    -> BuiltinOffsetofExpressionAST* {
+  node->value = value;
+  node->valueCategory = valueCategory;
+  node->type = type;
+  return node;
+}
+
+auto BuiltinOffsetofExpressionAST::create(
+    Arena* arena, TypeIdAST* typeId, List<DesignatorAST*>* designatorList,
+    const Identifier* identifier, FieldSymbol* symbol,
+    std::optional<std::uint64_t> value, ValueCategory valueCategory,
+    const Type* type) -> BuiltinOffsetofExpressionAST* {
   auto node = new (arena) BuiltinOffsetofExpressionAST();
   node->typeId = typeId;
   node->designatorList = designatorList;
   node->identifier = identifier;
   node->symbol = symbol;
+  node->value = value;
   node->valueCategory = valueCategory;
   node->type = type;
   return node;
diff --git a/src/parser/cxx/ast.h b/src/parser/cxx/ast.h
--- a/src/parser/cxx/ast.h
+++ b/src/parser/cxx/ast.h
@@ -4026,6 +4026,8 @@
   SourceLocation rparenLoc;
   const Identifier* identifier = nullptr;
   FieldSymbol* symbol = nullptr;
+  // Complete byte offset, absent until nondependent semantic checking succeeds.
+  std::optional<std::uint64_t> value;

   void accept(ASTVisitor* visitor) override { visitor->visit(this); }

@@ -4043,16 +4045,14 @@
       TypeIdAST* typeId, SourceLocation commaLoc, SourceLocation identifierLoc,
       List<DesignatorAST*>* designatorList, SourceLocation rparenLoc,
       const Identifier* identifier, FieldSymbol* symbol,
-      ValueCategory valueCategory, const Type* type)
-      -> BuiltinOffsetofExpressionAST*;
-
-  [[nodiscard]] static auto create(Arena* arena, TypeIdAST* typeId,
-                                   List<DesignatorAST*>* designatorList,
-                                   const Identifier* identifier,
-                                   FieldSymbol* symbol,
-                                   ValueCategory valueCategory,
-                                   const Type* type)
-      -> BuiltinOffsetofExpressionAST*;
+      std::optional<std::uint64_t> value, ValueCategory valueCategory,
+      const Type* type) -> BuiltinOffsetofExpressionAST*;
+
+  [[nodiscard]] static auto create(
+      Arena* arena, TypeIdAST* typeId, List<DesignatorAST*>* designatorList,
+      const Identifier* identifier, FieldSymbol* symbol,
+      std::optional<std::uint64_t> value, ValueCategory valueCategory,
+      const Type* type) -> BuiltinOffsetofExpressionAST*;

  protected:
   BuiltinOffsetofExpressionAST() : ExpressionAST(Kind) {}
diff --git a/src/parser/cxx/ast_interpreter_expressions.cc b/src/parser/cxx/ast_interpreter_expressions.cc
--- a/src/parser/cxx/ast_interpreter_expressions.cc
+++ b/src/parser/cxx/ast_interpreter_expressions.cc
@@ -1835,23 +1835,8 @@

 auto ASTInterpreter::ExpressionVisitor::operator()(
     BuiltinOffsetofExpressionAST* ast) -> ExpressionResult {
-  auto typeIdResult = interp.typeId(ast->typeId);
-
-  if (!ast->symbol) return std::nullopt;
-
-  auto classType = type_cast<ClassType>(ast->typeId->type);
-  if (!classType) return std::nullopt;
-
-  auto classSymbol = classType->symbol();
-  unit()->typeTraits().requireCompleteClass(classSymbol);
-  classSymbol = classSymbol->resolvedDefinition();
-  auto layout = classSymbol->layout();
-  if (!layout) return std::nullopt;
-
-  auto fieldInfo = layout->getFieldInfo(ast->symbol);
-  if (!fieldInfo) return std::nullopt;
-
-  return static_cast<int>(fieldInfo->offset);
+  if (!ast->value) return std::nullopt;
+  return static_cast<std::intmax_t>(*ast->value);
 }

 auto ASTInterpreter::ExpressionVisitor::operator()(TypeidExpressionAST* ast)
diff --git a/src/parser/cxx/dependent_types.cc b/src/parser/cxx/dependent_types.cc
--- a/src/parser/cxx/dependent_types.cc
+++ b/src/parser/cxx/dependent_types.cc
@@ -450,7 +450,10 @@
   [[nodiscard]] auto isDependent(RequirementAST* ast) -> bool;
   [[nodiscard]] auto isDependent(TypeConstraintAST* ast) -> bool;
   [[nodiscard]] auto isDependent(SplicerAST* ast) -> bool { return false; }
-  [[nodiscard]] auto isDependent(DesignatorAST* ast) -> bool { return false; }
+  [[nodiscard]] auto isDependent(DesignatorAST* ast) -> bool {
+    auto* subscript = ast_cast<SubscriptDesignatorAST>(ast);
+    return subscript && isDependent(subscript->expression);
+  }
   [[nodiscard]] auto isDependent(NewPlacementAST* ast) -> bool { return false; }
   [[nodiscard]] auto isDependent(DeclaratorAST* ast) -> bool { return false; }
   [[nodiscard]] auto isDependent(NewInitializerAST* ast) -> bool { return false; }
diff --git a/src/parser/cxx/private/semantic_model.h b/src/parser/cxx/private/semantic_model.h
--- a/src/parser/cxx/private/semantic_model.h
+++ b/src/parser/cxx/private/semantic_model.h
@@ -1357,6 +1357,8 @@
      FieldPersistence::kPersisted, ""},
     {"::cxx::BuiltinOffsetofExpressionAST", "symbol",
      FieldPersistence::kPersisted, ""},
+    {"::cxx::BuiltinOffsetofExpressionAST", "value",
+     FieldPersistence::kPersisted, ""},
     {"::cxx::TypeidExpressionAST", "typeidLoc", FieldPersistence::kPersisted,
      ""},
     {"::cxx::TypeidExpressionAST", "lparenLoc", FieldPersistence::kPersisted,
diff --git a/src/parser/cxx/semantic_codec.cc b/src/parser/cxx/semantic_codec.cc
--- a/src/parser/cxx/semantic_codec.cc
+++ b/src/parser/cxx/semantic_codec.cc
@@ -5089,6 +5089,11 @@
   out.varU32(static_cast<std::uint32_t>(identifierRef(self->identifier)));
   // ::cxx::BuiltinOffsetofExpressionAST::symbol
   out.varU32(static_cast<std::uint32_t>(symbolRef(self->symbol)));
+  // ::cxx::BuiltinOffsetofExpressionAST::value
+  out.boolean(self->value.has_value());
+  if (self->value.has_value()) {
+    out.varU64(static_cast<std::uint64_t>((*self->value)));
+  }
 }

 void SemanticEncoder::writeAstTypeidExpressionAST(
@@ -15060,6 +15065,13 @@
   cxx::FieldSymbol* value11 =
       symbol_cast<FieldSymbol>(symbolAt(SymbolRef{in.varU32()}));
   self->symbol = std::move(value11);
+  // ::cxx::BuiltinOffsetofExpressionAST::value
+  std::optional<std::uint64_t> offsetValue1;
+  if (in.boolean()) {
+    std::uint64_t offsetValue2 = static_cast<std::uint64_t>(in.varU64());
+    offsetValue1 = std::move(offsetValue2);
+  }
+  self->value = std::move(offsetValue1);
 }

 void SemanticDecoder::readAstTypeidExpressionAST(
diff --git a/src/parser/cxx/type_checker.cc b/src/parser/cxx/type_checker.cc
--- a/src/parser/cxx/type_checker.cc
+++ b/src/parser/cxx/type_checker.cc
@@ -49,6 +49,7 @@
 #include <bit>
 #include <cmath>
 #include <format>
+#include <limits>
 #include <string_view>
 #include <unordered_map>
 #include <unordered_set>
@@ -3875,44 +3876,111 @@

 void TypeChecker::Visitor::operator()(BuiltinOffsetofExpressionAST* ast) {
   ast->type = control()->getSizeType();
-  if (isDependent(check.unit_, ast->typeId)) return;
-
-  auto classType =
-      ast->typeId ? unqualified_cast<ClassType>(ast->typeId->type) : nullptr;
-
-  if (!classType) {
-    error(ast->firstSourceLocation(), "expected a type");
-    return;
-  }
-
-  if (!ast->identifier) {
-    return;
-  }
-
-  auto symbol = classType->symbol();
-  traits.requireCompleteClass(symbol);
-  symbol = symbol->resolvedDefinition();
-  auto member = qualifiedLookup(symbol, ast->identifier);
-
-  auto field = symbol_cast<FieldSymbol>(member);
-  if (!field) {
-    error(ast->firstSourceLocation(),
-          std::format("no member named '{}'", ast->identifier->name()));
-    return;
-  }
-
-  if (field->isBitField() || field->isStatic()) {
-    error(ast->firstSourceLocation(),
-          "offsetof requires an addressable data member");
-    return;
-  }
-  if (ast->designatorList) {
-    error(ast->firstSourceLocation(),
-          "compound offsetof designators are not supported");
-    return;
-  }
-
-  ast->symbol = field;
+  ast->value.reset();
+  if (isDependent(check.unit_, ast)) return;
+  if (!ast->typeId || !ast->typeId->type || !ast->identifier) return;
+
+  const auto* memoryLayout = control()->memoryLayout();
+  const auto maximumOffset = std::numeric_limits<std::uint64_t>::max() >>
+                             (64 - memoryLayout->sizeOfSizeType() * 8);
+  std::uint64_t offset = 0;
+  auto* currentType = ast->typeId->type;
+
+  // Resolve each member against its own enclosing record, retaining bindings
+  // for source inspection and the complete offset for constant consumers.
+  auto memberOffset = [&](const Identifier* identifier, SourceLocation location,
+                          FieldSymbol*& boundField) -> bool {
+    auto* classType = unqualified_cast<ClassType>(currentType);
+    if (!classType) {
+      error(location, "offsetof member designator requires a record type");
+      return false;
+    }
+    auto* record = classType->symbol();
+    if (!traits.requireCompleteClass(record)) {
+      error(location, "offsetof requires a complete record type");
+      return false;
+    }
+    record = record->resolvedDefinition();
+    auto* member = qualifiedLookup(record, identifier);
+    if (!member) {
+      error(location, std::format("no member named '{}'", identifier->name()));
+      return false;
+    }
+    auto* field = symbol_cast<FieldSymbol>(member);
+    if (!field || field->isBitField() || field->isStatic()) {
+      error(location, "offsetof requires an addressable data member");
+      return false;
+    }
+    auto* layout = record->layout();
+    auto position = layout ? layout->getFieldInfo(field) : std::nullopt;
+    if (!position) {
+      error(location, "offsetof member has no resolved record layout");
+      return false;
+    }
+    if (position->offset > maximumOffset - offset) {
+      error(location, "offsetof result exceeds source size_t");
+      return false;
+    }
+    offset += position->offset;
+    currentType = field->type();
+    boundField = field;
+    return true;
+  };
+
+  if (!memberOffset(ast->identifier, ast->identifierLoc, ast->symbol)) return;
+  for (auto* designator : ListView{ast->designatorList}) {
+    if (auto* member = ast_cast<DotDesignatorAST>(designator)) {
+      if (!member->identifier ||
+          !memberOffset(member->identifier, member->identifierLoc,
+                        member->symbol)) {
+        return;
+      }
+      continue;
+    }
+    auto* subscript = ast_cast<SubscriptDesignatorAST>(designator);
+    auto* elementType = traits.is_array(currentType)
+                            ? traits.remove_extent(currentType)
+                            : nullptr;
+    if (!elementType) {
+      error(subscript->firstSourceLocation(),
+            "offsetof subscript requires an array member");
+      return;
+    }
+    auto* expression = subscript->expression;
+    if (!expression || !traits.is_integral_or_enum(expression->type)) {
+      error(subscript->firstSourceLocation(),
+            "offsetof index must be an integral constant expression");
+      return;
+    }
+    ASTInterpreter interpreter(check.unit_, check.scope_);
+    auto constant = interpreter.evaluate(expression);
+    auto index = constant ? interpreter.toUInt(*constant) : std::nullopt;
+    if (!index) {
+      error(subscript->firstSourceLocation(),
+            "offsetof index must be an integral constant expression");
+      return;
+    }
+    auto representation = traits.integral_representation(expression->type);
+    if (representation->isSigned && interpreter.toInt(*constant).value() < 0) {
+      error(subscript->firstSourceLocation(),
+            "negative offsetof indices are not supported");
+      return;
+    }
+    auto stride = memoryLayout->sizeOf(elementType);
+    if (!stride) {
+      error(subscript->firstSourceLocation(),
+            "offsetof subscript requires a complete element type");
+      return;
+    }
+    if (*stride && *index > (maximumOffset - offset) / *stride) {
+      error(subscript->firstSourceLocation(),
+            "offsetof result exceeds source size_t");
+      return;
+    }
+    offset += *index * *stride;
+    currentType = elementType;
+  }
+  ast->value = offset;
 }

 void TypeChecker::Visitor::operator()(BuiltinBitCastExpressionAST* ast) {
